import { render } from '@testing-library/react'
import {{pascalCase name}} from './page'

describe('<{{pascalCase name}} />', () => {
  test('should display a blank login form, with remember me checked by default', async () => {
    render(<{{pascalCase name}} />)
  })
})